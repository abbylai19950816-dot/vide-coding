# 2026-05-14 恢復收費卡片狀態顯示

## 需求摘要

上次調整收費卡片時誤把價格下方的 `已收款` / `待收款` 狀態移除。使用者確認新版型應保留右側價格下方的狀態，並且姓名要恢復最原本的粗細與顏色；姓名與課程方案名稱需要靠左對齊。

## 變更檔案

- `active/gyrobooking_current/github_pages/admin.html`
- `admin.html`

## 行為變更

- 收費紀錄卡片右側恢復 `已收款` / `待收款` 狀態 badge。
- 待收款列表右側恢復 `待收款` 狀態 badge。
- 姓名樣式恢復原本的 `font-weight: 500` 與預設文字色。
- 姓名與課程方案名稱維持同一個左側起點；手機接在姓名右側。

## Firestore 讀寫影響

- 無新增 Firestore 讀取、寫入或 listener。
- 學員端無變更。
- 此次僅調整管理員端付款卡片渲染與樣式。

## 驗證

- 檢查收費紀錄與待收款卡片 template。
- 執行管理員頁 script 語法檢查。
- 執行低成本關鍵字檢查，確認沒有新增讀寫路徑。

## 後續風險

- 後續 UIUX 調整需先提供版型確認，再實作修改。
