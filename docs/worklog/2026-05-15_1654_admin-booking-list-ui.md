# 2026-05-15 16:54 學員預約列表 UI 微調

## 需求摘要

學員詳細頁的「預約課程時段」加入課程名稱後，同一列顯示日期、時間、課程名稱與三個操作按鈕，視覺上壅擠且上下兩筆排版不一致。使用者希望最多兩行，不要改成大卡片，按鈕靠右。

## 變更檔案

- `active/gyrobooking_current/github_pages/admin.html`
- `admin.html`
- `docs/worklog/2026-05-15_1654_admin-booking-list-ui.md`

## 行為變更

- 每筆未來預約改為最多兩行：
  - 第一行：日期、時間、課程名稱。
  - 第二行：`課程紀錄`、`改時間`、`取消` 三個按鈕靠右排列。
- 保留原本清單分隔線，不改成大卡片。
- 課程名稱過長時使用單行省略，避免擠壓按鈕或造成換行混亂。

## Firestore 讀寫影響

- 無 Firestore read/write 變更。
- 純 UI HTML 排版調整，不影響資料同步、取消預約、改時間或課程紀錄流程。

## 驗證

- 已同步 active 與根目錄 `admin.html`。
- 已對根目錄 `admin.html` 的 classic scripts 執行 `node --check`，4 段 script 均通過。

## 後續風險

- 如果未來課程名稱普遍很長，可再考慮讓第一行課程名稱改成自然換第二行，按鈕固定第三行；目前依使用者需求先維持最多兩行與高密度列表。
